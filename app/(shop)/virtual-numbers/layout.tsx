import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Virtual Phone Numbers',
  description: 'Get virtual phone numbers for SMS verification, privacy, and business communication. Numbers available worldwide.',
}

export default function VirtualNumbersLayout({ children }: { children: React.ReactNode }) {
  return children
}
